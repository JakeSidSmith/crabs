import { blue, cyan, green, magenta, red, yellow } from 'chalk';

export const PROGRAM = 'crabs';
export const UTF8 = 'utf8';
export const COLORS = [red, green, blue, magenta, cyan, yellow];
export const MATCHES_SPACES = /\s+/;
